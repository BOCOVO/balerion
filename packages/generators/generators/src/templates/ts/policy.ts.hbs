/**
 * {{ id }} policy
 */

export default (policyContext, config, { balerion }) => {
    // Add your own logic here.
    balerion.log.info('In {{ id }} policy.');

    const canDoSomething = true;

    if (canDoSomething) {
      return true;
    }

    return false;
};
